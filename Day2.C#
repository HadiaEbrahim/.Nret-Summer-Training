using System.Drawing;
using System.Security.Authentication;

namespace DAY2C_
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //1.Write a program that allows the user to enter a number then print it.
            Console.WriteLine("Enter a number:");
            int number;
            bool isValid = int.TryParse(Console.ReadLine(), out number);
            Console.WriteLine(number);


            //2.Write C# program that converts a string to an integer, but the string contains non-numeric characters. And mention what will happen
            Console.WriteLine("Enter a string:");
            string input = Console.ReadLine();
            int result;
            bool isParsed = int.TryParse(input, out result);
            if (isParsed)
            {
                Console.WriteLine("Parsed integer: " + result);
            }
            else
            {
                Console.WriteLine("Failed to parse the string as an integer.");
            } // This will fail to parse the string as an integer if it contains non-numeric characters, and the output will indicate that parsing failed.

            //3.Write C# program that take two string variables and print them as one variable 
            Console.WriteLine("Enter first string:");
            string str1 = Console.ReadLine();
            Console.WriteLine("Enter second string:");
            string str2 = Console.ReadLine();
            Console.WriteLine(str1 + str2); // Concatenates and prints the two strings as one variable

            //4.Write C# program that take two string variables and print them as one variable
            int principalValue, rate, time;
            Console.WriteLine("Enter principal value:");
            int.TryParse(Console.ReadLine(), out principalValue);
            Console.WriteLine("Enter rate:");
            int.TryParse(Console.ReadLine(), out rate);
            Console.WriteLine("Enter time:");
            int.TryParse(Console.ReadLine(), out time);

            int simpleInterest = (principalValue * rate * time) / 100;
            Console.WriteLine(simpleInterest);

            //5.Write a program that calculates the Body Mass Index (BMI) given a person's weight in kilograms and height in meters. 
            int weight, height;
            Console.WriteLine("Enter weight in kilograms:");
            int.TryParse(Console.ReadLine(), out weight);
            Console.WriteLine("Enter height in meters:");
            int.TryParse(Console.ReadLine(), out height);

            int BMI = weight / (height * height);
            Console.WriteLine(BMI);


            //6.Write a program that uses the ternary operator to check if the temperature is too hot, too cold, or just good. Assign the result in a variable then display the result. 
            int temperature;
            Console.WriteLine("Enter temperature:");
            int.TryParse(Console.ReadLine(), out temperature);
            if (temperature < 10)
            {
                Console.WriteLine("Just Cold");
            }
            else if (temperature > 30)
            {
                Console.WriteLine("Just Hot");
            }
            else
            {
                Console.WriteLine("Just Good");
            }

            //Write a program that takes the date from the user and displays it in various formats using string interpolation.
            int day,month,year;
            Console.WriteLine("enter the day");
            int.TryParse(Console.ReadLine(), out day);
            Console.WriteLine("enter the month");
            int.TryParse(Console.ReadLine(), out month);
            Console.WriteLine("enter the year");
            int.TryParse(Console.ReadLine(), out year);

            Console.WriteLine($"Today's date: {day} , {month} , {year}");
            Console.WriteLine($"Today's date: {day} / {month} / {year}");
            Console.WriteLine($"Today's date: {day} - {month} - {year}");

            //8.Write a program that takes a number from the user then print yes if that number can be divided by 3 and 4 otherwise print no.
            int num;
            int.TryParse((string)Console.ReadLine(), out num);
            if (num % 3 == 0 && num % 4 == 0)
            {
                Console.WriteLine("Yes");
            }
            else Console.WriteLine("No");

            //9.Write a program that allows the user to insert an integer then print negative if it is negative number otherwise print positive.
            int num2;
            int.TryParse(Console.ReadLine(), out num2);
            if (num2 < 0) Console.WriteLine("Negative");
            else Console.WriteLine("Positive");

            //10.- Write a program that takes 3 integers from the user then prints the max element and the min element.
            int a, b, c;
            int.TryParse(Console.ReadLine(), out a);
            int.TryParse(Console.ReadLine(), out b);
            int.TryParse(Console.ReadLine(), out c);
            int min=a, max=b;
            if (a > max) max = a;
            if(c> max) max = c;
            if (b < min) min = b;
            if (c<min) min = c;
            Console.WriteLine($"Max element = {max}");
            Console.WriteLine($"Min element = {min}");

            //11.Write a program that allows the user to insert an integer number then check If a number is even or odd.
            int num3;
            int.TryParse(Console.ReadLine(), out num3);
            if (num3 % 2 == 0) Console.WriteLine("Even");
            else Console.WriteLine("Odd");

            //12.Write a program that takes character from the user then if it is a vowel chars (a,e,I,o,u) then print (vowel) otherwise print (consonant).
            char n;
            char.TryParse(Console.ReadLine(), out n);
            if (n == 'a' || n == 'e' || n == 'i' || n == 'o' || n == 'u') Console.WriteLine("vowel");
            else Console.WriteLine("Consonant");

            //13.Write a program to input the month number and print the number of days in that month.
            int mon;
            int.TryParse(Console.ReadLine(), out mon);
            switch (mon) {
                case 1:
                    Console.WriteLine(31); break;   
                case 2:
                    Console.WriteLine(28); break;
                case 3:
                    Console.WriteLine(31); break;
                case 4:
                    Console.WriteLine(28); break;
            }//and so on

            //Looping
            //- Write a program that allows the user to insert an integer then print all numbers between 1 to that number.
            int k;
            int.TryParse(Console.ReadLine(), out k);
            for (int i = 1; i<= k;i++) Console.WriteLine(i);

            //2- Write a program that allows the user to insert an integer then 
            //print a multiplication table up to 12.
            int m;
            int.TryParse(Console.ReadLine(), out m);
            for (int i = 1; i <= 12; i++) Console.WriteLine(i*m);


            //3- Write a program that allows to user to insert number then print all even numbers between 1 to this number
            int l;
            int.TryParse(Console.ReadLine(), out l);
            for (int i = 2; i <= l; i += 2) Console.WriteLine(i);

            //4- Write a program to enter marks of five subjects and calculate total, average and percentage.
            Console.WriteLine("Enter Marks of five subjects");
            int s1, s2, s3, s4, s5;
            int.TryParse(Console.ReadLine(), out s1);
            int.TryParse(Console.ReadLine(), out s2);
            int.TryParse(Console.ReadLine(), out s3);
            int.TryParse(Console.ReadLine(), out s4);
            int.TryParse(Console.ReadLine(), out s5);
            int sum=s1+s2+s3+s4+s5;
            int avg = sum / 5;
            Console.WriteLine($"Total marks {sum}");
            Console.WriteLine($"Average Marks  {avg}");
            Console.WriteLine($"Percentage {avg}");

        }
    }
}
